import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import { Root } from "./MainStack";

const Navigation = () => {
  return (
    <NavigationContainer>
      <Root />
    </NavigationContainer>
  );
};

export default Navigation;
